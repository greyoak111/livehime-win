From 04b9f98d911b1e34be3b09699401449bff202f23 Mon Sep 17 00:00:00 2001
From: greyoak111 <151205158+greyoak111@users.noreply.github.com>
Date: Thu, 24 Sep 2026 02:44:30 +0800
Subject: [PATCH 19/53] Add password / SMS login through Bilibili's official
 page

WebLoginWindow hosts mini-login-v2 (the page the Windows client uses) in
a non-persistent WebKit store with v0.1.2's host bridge: window.browser
context and the livehime_login shim at document start, biliBridgePc after
load, messages accepted only from the live.bilibili.com main frame. The
page handles passwords, captchas, SMS and secondary verification. Once the
session cookies are complete they are saved to the Keychain and verified
exactly like a QR login. v0.1.2's bridge contract and its tests move into
the core.

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
---
 plugins/livehime/CMakeLists.txt               |   3 +
 .../LiveHimeCore/AuthBridgeContract.swift     |  97 +++++++++
 .../LiveHimeCore/AuthWebViewBridge.swift      |  80 +++++++
 .../core/Sources/LiveHimeCore/CBridge.swift   |  13 ++
 .../LiveHimeCore/SessionCoordinator.swift     |  10 +
 .../Sources/LiveHimeCore/WebLoginWindow.swift | 201 ++++++++++++++++++
 .../AuthBridgeContractTests.swift             | 104 +++++++++
 .../AuthWebViewBridgeTests.swift              |  40 ++++
 .../SessionCoordinatorTests.swift             |  17 ++
 plugins/livehime/core/include/livehime-core.h |   3 +
 plugins/livehime/data/locale/en-US.ini        |   1 +
 plugins/livehime/data/locale/zh-CN.ini        |   1 +
 plugins/livehime/src/account-panel.cpp        |   8 +
 13 files changed, 578 insertions(+)
 create mode 100644 plugins/livehime/core/Sources/LiveHimeCore/AuthBridgeContract.swift
 create mode 100644 plugins/livehime/core/Sources/LiveHimeCore/AuthWebViewBridge.swift
 create mode 100644 plugins/livehime/core/Sources/LiveHimeCore/WebLoginWindow.swift
 create mode 100644 plugins/livehime/core/Tests/LiveHimeCoreTests/AuthBridgeContractTests.swift
 create mode 100644 plugins/livehime/core/Tests/LiveHimeCoreTests/AuthWebViewBridgeTests.swift

diff --git a/plugins/livehime/CMakeLists.txt b/plugins/livehime/CMakeLists.txt
index 1799c99d5..75cf6666a 100644
--- a/plugins/livehime/CMakeLists.txt
+++ b/plugins/livehime/CMakeLists.txt
@@ -9,6 +9,8 @@ target_sources(
   livehime
   PRIVATE
     core/include/livehime-core.h
+    core/Sources/LiveHimeCore/AuthBridgeContract.swift
+    core/Sources/LiveHimeCore/AuthWebViewBridge.swift
     core/Sources/LiveHimeCore/BilibiliControlClient.swift
     core/Sources/LiveHimeCore/BilibiliPageWindow.swift
     core/Sources/LiveHimeCore/BilibiliLiveClient.swift
@@ -21,6 +23,7 @@ target_sources(
     core/Sources/LiveHimeCore/PlatformSupport.swift
     core/Sources/LiveHimeCore/QRLogin.swift
     core/Sources/LiveHimeCore/SessionMaintenance.swift
+    core/Sources/LiveHimeCore/WebLoginWindow.swift
     core/Sources/LiveHimeCore/SessionCoordinator.swift
     src/account-panel.cpp
     src/account-panel.hpp
diff --git a/plugins/livehime/core/Sources/LiveHimeCore/AuthBridgeContract.swift b/plugins/livehime/core/Sources/LiveHimeCore/AuthBridgeContract.swift
new file mode 100644
index 000000000..dc5348c5e
--- /dev/null
+++ b/plugins/livehime/core/Sources/LiveHimeCore/AuthBridgeContract.swift
@@ -0,0 +1,97 @@
+import Foundation
+
+/// Privileged login callbacks are accepted only from our top-level official page.
+/// The face-auth window has no native login handler; its origin cannot inherit one.
+public enum AuthBridgeOriginPolicy {
+    public static func allows(scheme: String, host: String, port: Int, isMainFrame: Bool) -> Bool {
+        isMainFrame && scheme.lowercased() == "https" &&
+            host.lowercased() == "live.bilibili.com" && (port == 0 || port == 443)
+    }
+}
+
+public enum AuthBridgeContractError: String, Error {
+    case malformedPayload = "malformed_payload"
+    case unsupportedAction = "unsupported_action"
+    case untrustedOrigin = "untrusted_origin"
+}
+
+public enum NativeAuthRequest {
+    case login([String: Any])
+    case cookies(AuthCookieBatch)
+
+    public static func parse(action: String?, payload: Any?) throws -> NativeAuthRequest {
+        switch action {
+        case "auth/setRefreshToken":
+            let object: [String: Any]?
+            if let text = payload as? String, let data = text.data(using: .utf8) {
+                object = (try? JSONSerialization.jsonObject(with: data)) as? [String: Any]
+            } else {
+                object = payload as? [String: Any]
+            }
+            guard let object, !object.isEmpty else { throw AuthBridgeContractError.malformedPayload }
+            return .login(object)
+        case "auth/setCookies": return .cookies(try AuthCookieBatch(payload: payload))
+        default: throw AuthBridgeContractError.unsupportedAction
+        }
+    }
+}
+
+/// Validate the whole batch before writing anything. Cookies remain confined to
+/// bilibili.com; no source domain, payload, or value is sent to diagnostics.
+public struct AuthCookieBatch {
+    public struct Operation {
+        public let cookie: HTTPCookie
+        public let remove: Bool
+    }
+    public let operations: [Operation]
+
+    public init(payload: Any?, now: Date = Date()) throws {
+        guard let items = payload as? [[String: Any]], items.count <= 128 else {
+            throw AuthBridgeContractError.malformedPayload
+        }
+        operations = try items.map { item in
+            guard let name = item["name"] as? String, let value = item["value"] as? String,
+                  !name.isEmpty, name.utf8.count <= 256, value.utf8.count <= 16384,
+                  name.rangeOfCharacter(from: .controlCharacters) == nil,
+                  value.rangeOfCharacter(from: .controlCharacters) == nil,
+                  !name.contains(";"), !name.contains("=") else {
+                throw AuthBridgeContractError.malformedPayload
+            }
+            var properties: [HTTPCookiePropertyKey: Any] = [
+                .domain: ".bilibili.com", .path: "/", .name: name, .value: value,
+                .secure: "TRUE"
+            ]
+            var remove = false
+            if let expiry = item["expirationDate"] as? NSNumber {
+                guard expiry.doubleValue.isFinite else { throw AuthBridgeContractError.malformedPayload }
+                properties[.expires] = Date(timeIntervalSince1970: expiry.doubleValue)
+                remove = expiry.doubleValue <= now.timeIntervalSince1970 &&
+                    (item["isExpiredRemove"] as? Bool) == true
+            }
+            guard let cookie = HTTPCookie(properties: properties) else {
+                throw AuthBridgeContractError.malformedPayload
+            }
+            return Operation(cookie: cookie, remove: remove)
+        }
+    }
+}
+
+extension AuthWebViewBridge {
+    /// Inject after navigation finishes, preserving the v0.1.0 render order.
+    /// WKScriptMessageHandlerWithReply acknowledges completed cookie writes.
+    /// Acknowledgement means host handling finished, not that login is valid.
+    public static let nativeAuthBridgeScript = """
+    (() => {
+      if (window.biliBridgePc) return false;
+      window.biliBridgePc = {
+        callNative: (action, payload) => {
+          if (!['auth/setRefreshToken', 'auth/setCookies'].includes(action)) {
+            return Promise.reject(new Error('unsupported_action'));
+          }
+          return window.webkit.messageHandlers.livehime_native.postMessage({action, payload});
+        }
+      };
+      return true;
+    })();
+    """
+}
diff --git a/plugins/livehime/core/Sources/LiveHimeCore/AuthWebViewBridge.swift b/plugins/livehime/core/Sources/LiveHimeCore/AuthWebViewBridge.swift
new file mode 100644
index 000000000..23a5f9f2d
--- /dev/null
+++ b/plugins/livehime/core/Sources/LiveHimeCore/AuthWebViewBridge.swift
@@ -0,0 +1,80 @@
+import Foundation
+
+public enum AuthWebViewEvent: Equatable, Sendable {
+    case succeeded(LoginResult)
+    case cancelled
+    case secondaryValidationReturned
+    case resize(width: Double, height: Double)
+}
+
+public struct LoginResult: Equatable, Sendable {
+    public let type: String?
+    public let refreshToken: String?
+    public let timestamp: String?
+    public let url: URL?
+
+    public init(type: String? = nil, refreshToken: String? = nil, timestamp: String? = nil, url: URL? = nil) {
+        self.type = type
+        self.refreshToken = refreshToken
+        self.timestamp = timestamp
+        self.url = url
+    }
+}
+
+/// Host-side contract for the official mini-login page.
+/// WKWebView integration should forward a JS shim through WKScriptMessageHandler.
+public protocol AuthWebViewBridgeDelegate: AnyObject {
+    func authBridge(_ bridge: AuthWebViewBridge, didEmit event: AuthWebViewEvent)
+}
+
+public final class AuthWebViewBridge: @unchecked Sendable {
+    /// JavaScript to inject at document start when hosting the page in WKWebView.
+    /// The native side receives messages through WKScriptMessageHandler named `livehime_login`.
+    public static let wkWebViewShim = """
+    (() => {
+      window.livehime_login = {
+        LoginSuccess: data => webkit.messageHandlers.livehime_login.postMessage({method: 'LoginSuccess', data}),
+        Cancel: () => webkit.messageHandlers.livehime_login.postMessage({method: 'Cancel'}),
+        SecondaryValidationResult: () => webkit.messageHandlers.livehime_login.postMessage({method: 'SecondaryValidationResult'}),
+        SwitchLogin: (width, height) => webkit.messageHandlers.livehime_login.postMessage({method: 'SwitchLogin', width, height})
+      };
+    })();
+    """
+
+    public weak var delegate: AuthWebViewBridgeDelegate?
+    public var onEvent: ((AuthWebViewEvent) -> Void)?
+
+    public init(delegate: AuthWebViewBridgeDelegate? = nil) {
+        self.delegate = delegate
+    }
+
+    public func loginSuccess(_ payload: [String: Any]) {
+        let result = LoginResult(
+            type: payload["type"] as? String,
+            refreshToken: (payload["refresh_token"] as? String)
+                ?? (payload["refreshToken"] as? String)
+                ?? (payload["token"] as? String),
+            timestamp: payload["timestamp"].map(String.init(describing:)),
+            url: (payload["url"] as? String).flatMap(URL.init(string:))
+        )
+        emit(.succeeded(result))
+    }
+
+    public func cancel() {
+        emit(.cancelled)
+    }
+
+    public func secondaryValidationResult() {
+        emit(.secondaryValidationReturned)
+    }
+
+    public func switchLogin(width: Double, height: Double) {
+        guard width.isFinite, height.isFinite, width > 0, height > 0 else { return }
+        emit(.resize(width: width, height: height))
+    }
+
+    private func emit(_ event: AuthWebViewEvent) {
+        delegate?.authBridge(self, didEmit: event)
+        onEvent?(event)
+    }
+}
diff --git a/plugins/livehime/core/Sources/LiveHimeCore/CBridge.swift b/plugins/livehime/core/Sources/LiveHimeCore/CBridge.swift
index 67f92d30e..2560a2809 100644
--- a/plugins/livehime/core/Sources/LiveHimeCore/CBridge.swift
+++ b/plugins/livehime/core/Sources/LiveHimeCore/CBridge.swift
@@ -339,3 +339,16 @@ public func livehime_core_set_shield_keyword(_ keyword: UnsafePointer<CChar>?, _
         }
     }
 }
+
+/// Opens the official password / SMS login page. On success the session is
+/// saved and verified like a QR login (state events follow as usual).
+@_cdecl("livehime_core_start_web_login")
+public func livehime_core_start_web_login() {
+    onMain {
+        guard CoreBridge.shared != nil else { return }
+        CoreBridge.shared?.coordinator.cancelQRLogin()
+        WebLoginWindow.present { credentials in
+            CoreBridge.shared?.coordinator.completeWebLogin(credentials)
+        }
+    }
+}
diff --git a/plugins/livehime/core/Sources/LiveHimeCore/SessionCoordinator.swift b/plugins/livehime/core/Sources/LiveHimeCore/SessionCoordinator.swift
index 65f46af71..06cffaf9f 100644
--- a/plugins/livehime/core/Sources/LiveHimeCore/SessionCoordinator.swift
+++ b/plugins/livehime/core/Sources/LiveHimeCore/SessionCoordinator.swift
@@ -100,6 +100,16 @@ public final class SessionCoordinator {
         run { await self.qrLogin() }
     }
 
+    /// Credentials from the official web login (password / SMS): saved and
+    /// verified exactly like a QR login.
+    public func completeWebLogin(_ fresh: SessionCredentials) {
+        run {
+            do { try self.store.save(fresh) }
+            catch { self.state = .failed(.keychain, detail: "\(error)"); return }
+            await self.verify(fresh)
+        }
+    }
+
     public func cancelQRLogin() {
         task?.cancel()
         task = nil
diff --git a/plugins/livehime/core/Sources/LiveHimeCore/WebLoginWindow.swift b/plugins/livehime/core/Sources/LiveHimeCore/WebLoginWindow.swift
new file mode 100644
index 000000000..7cc6af111
--- /dev/null
+++ b/plugins/livehime/core/Sources/LiveHimeCore/WebLoginWindow.swift
@@ -0,0 +1,201 @@
+import AppKit
+import CryptoKit
+import WebKit
+
+/// Password / SMS login through Bilibili's official mini-login page, the
+/// page the Windows client hosts in CEF. The page handles passwords,
+/// captchas (geetest), SMS and secondary verification itself; the window
+/// only provides the host bridge the page expects (`livehime_login` and
+/// `biliBridgePc`, as in v0.1.2) and collects the resulting session cookies.
+///
+/// The page runs in a non-persistent WebKit store. On success the cookies
+/// become SessionCredentials for the Keychain and the page data is dropped.
+@MainActor
+final class WebLoginWindow: NSObject, NSWindowDelegate, WKNavigationDelegate, WKScriptMessageHandler,
+                            WKScriptMessageHandlerWithReply {
+    private static var current: WebLoginWindow?
+    static let loginURL = URL(string: "https://live.bilibili.com/p/html/live-pc-blink/mini-login-v2/")!
+
+    private let window: NSWindow
+    private let webView: WKWebView
+    private let bridge = AuthWebViewBridge()
+    private let onComplete: (SessionCredentials) -> Void
+    private var refreshToken = ""
+    private var completed = false
+
+    static func present(onComplete: @escaping (SessionCredentials) -> Void) {
+        if let current {
+            current.window.makeKeyAndOrderFront(nil)
+            return
+        }
+        current = WebLoginWindow(onComplete: onComplete)
+    }
+
+    static func close() {
+        current?.window.close()
+    }
+
+    private init(onComplete: @escaping (SessionCredentials) -> Void) {
+        self.onComplete = onComplete
+        let content = WKUserContentController()
+        let configuration = WKWebViewConfiguration()
+        configuration.userContentController = content
+        configuration.websiteDataStore = .nonPersistent()
+        webView = WKWebView(frame: .zero, configuration: configuration)
+        window = NSWindow(contentRect: NSRect(x: 0, y: 0, width: 820, height: 520),
+                          styleMask: [.titled, .closable, .miniaturizable], backing: .buffered, defer: false)
+        super.init()
+
+        // The Windows host defines `window.browser` before the page loads;
+        // the page copies it into cookies and checks it after verification.
+        content.addUserScript(WKUserScript(source: Self.browserContextScript(), injectionTime: .atDocumentStart,
+                                           forMainFrameOnly: true))
+        content.addUserScript(WKUserScript(source: AuthWebViewBridge.wkWebViewShim, injectionTime: .atDocumentStart,
+                                           forMainFrameOnly: true))
+        content.add(self, name: "livehime_login")
+        content.addScriptMessageHandler(self, contentWorld: .page, name: "livehime_native")
+        bridge.onEvent = { [weak self] event in self?.handle(event) }
+
+        webView.navigationDelegate = self
+        window.title = "哔哩哔哩登录"
+        window.contentMinSize = NSSize(width: 820, height: 460)
+        window.contentView = webView
+        window.isReleasedWhenClosed = false
+        window.delegate = self
+        window.center()
+        window.makeKeyAndOrderFront(nil)
+        NSApp.activate(ignoringOtherApps: true)
+        webView.load(URLRequest(url: Self.loginURL, cachePolicy: .reloadIgnoringLocalAndRemoteCacheData))
+    }
+
+    /// Same values v0.1.2 used; buvid3 is kept per install.
+    static func browserContextScript() -> String {
+        let key = "livehime.buvid3"
+        let buvid3: String
+        if let saved = UserDefaults.standard.string(forKey: key), !saved.isEmpty {
+            buvid3 = saved
+        } else {
+            buvid3 = UUID().uuidString.replacingOccurrences(of: "-", with: "").lowercased()
+            UserDefaults.standard.set(buvid3, forKey: key)
+        }
+        let context = ["appkey": "aae92bc66f3edfab", "buvid3": buvid3, "device_name": "macOS", "device_platform": "mac"]
+        let json = (try? JSONSerialization.data(withJSONObject: context)).map { String(decoding: $0, as: UTF8.self) } ?? "{}"
+        return "window.browser = Object.assign({}, window.browser || {}, \(json));"
+    }
+
+    // MARK: Bridge
+
+    private func accepts(_ message: WKScriptMessage) -> Bool {
+        let origin = message.frameInfo.securityOrigin
+        return message.webView === webView &&
+            AuthBridgeOriginPolicy.allows(scheme: origin.protocol, host: origin.host, port: origin.port,
+                                          isMainFrame: message.frameInfo.isMainFrame)
+    }
+
+    func userContentController(_ controller: WKUserContentController, didReceive message: WKScriptMessage) {
+        guard accepts(message), message.name == "livehime_login",
+              let body = message.body as? [String: Any], let method = body["method"] as? String else { return }
+        switch method {
+        case "LoginSuccess": bridge.loginSuccess(Self.dictionary(body["data"]))
+        case "Cancel": bridge.cancel()
+        case "SecondaryValidationResult": bridge.secondaryValidationResult()
+        case "SwitchLogin": bridge.switchLogin(width: body["width"] as? Double ?? 0, height: body["height"] as? Double ?? 0)
+        default: break
+        }
+    }
+
+    func userContentController(_ controller: WKUserContentController, didReceive message: WKScriptMessage,
+                               replyHandler: @escaping (Any?, String?) -> Void) {
+        guard accepts(message), message.name == "livehime_native", let body = message.body as? [String: Any] else {
+            replyHandler(nil, AuthBridgeContractError.untrustedOrigin.rawValue)
+            return
+        }
+        do {
+            switch try NativeAuthRequest.parse(action: body["action"] as? String, payload: body["payload"]) {
+            case .login(let result):
+                bridge.loginSuccess(result)
+                replyHandler(["accepted": true], nil)
+            case .cookies(let batch):
+                let store = webView.configuration.websiteDataStore.httpCookieStore
+                Task { @MainActor in
+                    for operation in batch.operations {
+                        if operation.remove { await store.deleteCookie(operation.cookie) } else { await store.setCookie(operation.cookie) }
+                    }
+                    replyHandler(["accepted": true], nil)
+                }
+            }
+        } catch let error as AuthBridgeContractError {
+            replyHandler(nil, error.rawValue)
+        } catch {
+            replyHandler(nil, AuthBridgeContractError.malformedPayload.rawValue)
+        }
+    }
+
+    /// The page may pass the payload as an object or as a JSON string.
+    static func dictionary(_ value: Any?) -> [String: Any] {
+        if let object = value as? [String: Any] { return object }
+        if let text = value as? String, let data = text.data(using: .utf8),
+           let object = try? JSONSerialization.jsonObject(with: data) as? [String: Any] { return object }
+        return [:]
+    }
+
+    private func handle(_ event: AuthWebViewEvent) {
+        switch event {
+        case .succeeded(let result):
+            if let token = result.refreshToken, !token.isEmpty {
+                refreshToken = token
+                if let timestamp = result.timestamp { seedCookies(token: token, timestamp: timestamp) }
+            }
+            collectCookies()
+        case .secondaryValidationReturned:
+            // The page redirects back after verification; the cookie jar is
+            // already authenticated at this point.
+            collectCookies()
+        case .cancelled:
+            window.close()
+        case .resize(let width, let height):
+            window.setContentSize(NSSize(width: max(820, min(width, 1200)), height: max(460, min(height, 900))))
+        }
+    }
+
+    /// The page hands back a refresh token; loading correspond/0 with this
+    /// digest asks Bilibili to set the matching cookies (v0.1.2 behaviour).
+    private func seedCookies(token: String, timestamp: String) {
+        let marker = "set_\(timestamp)_\(token)"
+        let hex = marker.unicodeScalars.map { String($0.value, radix: 16) }.joined()
+        let digest = SHA256.hash(data: Data(hex.utf8)).map { String(format: "%02x", $0) }.joined()
+        guard let literal = try? JSONSerialization.data(withJSONObject: "https://www.bilibili.com/correspond/0/\(digest)",
+                                                         options: .fragmentsAllowed) else { return }
+        let script = "(() => { const f=document.createElement('iframe'); f.style.display='none'; f.src=\(String(decoding: literal, as: UTF8.self)); document.body.appendChild(f); })();"
+        webView.evaluateJavaScript(script, completionHandler: nil)
+    }
+
+    /// Cookies can arrive shortly after the success callback; poll briefly.
+    private func collectCookies(attempt: Int = 0) {
+        guard !completed else { return }
+        webView.configuration.websiteDataStore.httpCookieStore.getAllCookies { [weak self] cookies in
+            guard let self, !self.completed else { return }
+            var jar: [String: String] = [:]
+            for cookie in cookies where cookie.domain.hasSuffix("bilibili.com") { jar[cookie.name] = cookie.value }
+            let credentials = SessionCredentials(cookies: jar, refreshToken: self.refreshToken)
+            if credentials.isComplete {
+                self.completed = true
+                self.onComplete(credentials)
+                self.window.close()
+            } else if attempt < 20 {
+                DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) { self.collectCookies(attempt: attempt + 1) }
+            }
+        }
+    }
+
+    // MARK: WKNavigationDelegate / NSWindowDelegate
+
+    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
+        webView.evaluateJavaScript(AuthWebViewBridge.nativeAuthBridgeScript, completionHandler: nil)
+    }
+
+    func windowWillClose(_ notification: Notification) {
+        webView.stopLoading()
+        Self.current = nil
+    }
+}
diff --git a/plugins/livehime/core/Tests/LiveHimeCoreTests/AuthBridgeContractTests.swift b/plugins/livehime/core/Tests/LiveHimeCoreTests/AuthBridgeContractTests.swift
new file mode 100644
index 000000000..1b45bf363
--- /dev/null
+++ b/plugins/livehime/core/Tests/LiveHimeCoreTests/AuthBridgeContractTests.swift
@@ -0,0 +1,104 @@
+import XCTest
+import WebKit
+@testable import LiveHimeCore
+
+final class AuthBridgeContractTests: XCTestCase {
+    func testOnlyExactSecureMainFrameOriginIsPrivileged() {
+        XCTAssertTrue(AuthBridgeOriginPolicy.allows(scheme: "https", host: "live.bilibili.com", port: 443, isMainFrame: true))
+        for host in ["bilibili.com.attacker.invalid", "evil.live.bilibili.com", "passport.bilibili.com", ""] {
+            XCTAssertFalse(AuthBridgeOriginPolicy.allows(scheme: "https", host: host, port: 443, isMainFrame: true))
+        }
+        XCTAssertFalse(AuthBridgeOriginPolicy.allows(scheme: "http", host: "live.bilibili.com", port: 80, isMainFrame: true))
+        XCTAssertFalse(AuthBridgeOriginPolicy.allows(scheme: "https", host: "live.bilibili.com", port: 443, isMainFrame: false))
+        XCTAssertFalse(AuthBridgeOriginPolicy.allows(scheme: "https", host: "live.bilibili.com", port: 8443, isMainFrame: true))
+    }
+
+    func testUnknownAndMalformedActionsAreRejected() {
+        XCTAssertThrowsError(try NativeAuthRequest.parse(action: "other/operation", payload: [:]))
+        XCTAssertThrowsError(try NativeAuthRequest.parse(action: "auth/setRefreshToken", payload: "not-json"))
+        XCTAssertThrowsError(try NativeAuthRequest.parse(action: "auth/setCookies", payload: [["name": "missing-value"]]))
+    }
+
+    func testCookieBatchRejectsPartialAndUnboundedInput() throws {
+        XCTAssertThrowsError(try AuthCookieBatch(payload: [["name": "ok", "value": "fixture"], ["name": "bad"]]))
+        XCTAssertThrowsError(try AuthCookieBatch(payload: [["name": "x\r\n", "value": "fixture"]]))
+        XCTAssertThrowsError(try AuthCookieBatch(payload: Array(repeating: ["name": "x", "value": "v"], count: 129)))
+        let batch = try AuthCookieBatch(payload: [["name": "fixture", "value": "local", "domain": "attacker.invalid",
+            "expirationDate": 1, "isExpiredRemove": true]], now: Date(timeIntervalSince1970: 2))
+        XCTAssertEqual(batch.operations.first?.cookie.domain, ".bilibili.com")
+        XCTAssertEqual(batch.operations.first?.remove, true)
+        XCTAssertEqual(batch.operations.first?.cookie.isSecure, true)
+    }
+
+    @MainActor
+    func testRealWebKitWaitsForNativeAcknowledgementAndRejectsUnknownAction() async throws {
+        _ = NSApplication.shared
+        let sink = WebKitFixtureSink()
+        let config = WKWebViewConfiguration()
+        config.websiteDataStore = .nonPersistent()
+        config.userContentController.add(sink, name: "livehime_login")
+        config.userContentController.addScriptMessageHandler(sink, contentWorld: .page, name: "livehime_native")
+        config.userContentController.addUserScript(WKUserScript(source: AuthWebViewBridge.wkWebViewShim,
+            injectionTime: .atDocumentStart, forMainFrameOnly: true))
+        let view = WKWebView(frame: CGRect(x: 0, y: 0, width: 820, height: 520), configuration: config)
+        view.navigationDelegate = sink
+        defer {
+            view.stopLoading()
+            config.userContentController.removeAllScriptMessageHandlers()
+        }
+        view.loadHTMLString("<meta http-equiv='Content-Security-Policy' content=\"default-src 'none'; script-src 'unsafe-inline'\"><p>Offline bridge fixture</p>", baseURL: nil)
+        await fulfillment(of: [sink.loaded], timeout: 10)
+        let first = try await view.evaluateJavaScript(AuthWebViewBridge.nativeAuthBridgeScript) as? Bool
+        let repeated = try await view.evaluateJavaScript(AuthWebViewBridge.nativeAuthBridgeScript) as? Bool
+        XCTAssertEqual(first, true)
+        XCTAssertEqual(repeated, false)
+        let supported = try await view.callAsyncJavaScript("""
+            return await biliBridgePc.callNative('auth/setCookies', [{name:'fixture',value:'local'}]);
+            """, arguments: [:], in: nil, contentWorld: .page) as? [String: Bool]
+        XCTAssertEqual(supported?["accepted"], true)
+        XCTAssertTrue(sink.completedWrite, "The Promise must not resolve before the asynchronous native work")
+        let calls = sink.nativeCalls
+        let error = try await view.callAsyncJavaScript("""
+            try { await biliBridgePc.callNative('future/unsupported', {secret:'never-export'}); return 'wrong'; }
+            catch (error) { return error.message; }
+            """, arguments: [:], in: nil, contentWorld: .page) as? String
+        XCTAssertEqual(error, "unsupported_action")
+        XCTAssertEqual(sink.nativeCalls, calls)
+        _ = try await view.evaluateJavaScript("livehime_login.LoginSuccess({type:'fixture'});livehime_login.SecondaryValidationResult();livehime_login.Cancel();livehime_login.SwitchLogin(820,520);true;")
+        await fulfillment(of: [sink.legacyEvents], timeout: 5)
+        XCTAssertEqual(sink.methods, ["LoginSuccess", "SecondaryValidationResult", "Cancel", "SwitchLogin"])
+    }
+}
+
+@MainActor
+private final class WebKitFixtureSink: NSObject, WKNavigationDelegate, WKScriptMessageHandler, WKScriptMessageHandlerWithReply {
+    let loaded = XCTestExpectation(description: "local document loaded")
+    let legacyEvents: XCTestExpectation = {
+        let result = XCTestExpectation(description: "legacy callbacks received")
+        result.expectedFulfillmentCount = 4
+        return result
+    }()
+    var methods: [String] = []
+    var nativeCalls = 0
+    var completedWrite = false
+    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) { loaded.fulfill() }
+    func webView(_ webView: WKWebView, decidePolicyFor action: WKNavigationAction,
+                 decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
+        decisionHandler(action.request.url?.scheme == "about" ? .allow : .cancel)
+    }
+    func userContentController(_ controller: WKUserContentController, didReceive message: WKScriptMessage) {
+        if let body = message.body as? [String: Any], let method = body["method"] as? String {
+            methods.append(method); legacyEvents.fulfill()
+        }
+    }
+    func userContentController(_ controller: WKUserContentController, didReceive message: WKScriptMessage,
+                               replyHandler: @escaping (Any?, String?) -> Void) {
+        nativeCalls += 1
+        Task { @MainActor in
+            // Simulate the asynchronous cookie-store completion without network or Keychain.
+            try? await Task.sleep(nanoseconds: 100_000_000)
+            completedWrite = true
+            replyHandler(["accepted": true], nil)
+        }
+    }
+}
diff --git a/plugins/livehime/core/Tests/LiveHimeCoreTests/AuthWebViewBridgeTests.swift b/plugins/livehime/core/Tests/LiveHimeCoreTests/AuthWebViewBridgeTests.swift
new file mode 100644
index 000000000..4e99a75c6
--- /dev/null
+++ b/plugins/livehime/core/Tests/LiveHimeCoreTests/AuthWebViewBridgeTests.swift
@@ -0,0 +1,40 @@
+import XCTest
+@testable import LiveHimeCore
+
+final class AuthWebViewBridgeTests: XCTestCase {
+    final class Sink: AuthWebViewBridgeDelegate {
+        var events: [AuthWebViewEvent] = []
+        func authBridge(_ bridge: AuthWebViewBridge, didEmit event: AuthWebViewEvent) { events.append(event) }
+    }
+
+    func testLoginEventsAndInvalidResize() {
+        let sink = Sink()
+        let bridge = AuthWebViewBridge(delegate: sink)
+        bridge.loginSuccess(["type": "scan", "refresh_token": "opaque", "timestamp": 123, "url": "https://example.invalid/ok"])
+        bridge.cancel()
+        bridge.secondaryValidationResult()
+        bridge.switchLogin(width: 800, height: 600)
+        bridge.switchLogin(width: .nan, height: 600)
+        XCTAssertEqual(sink.events.count, 4)
+        XCTAssertEqual(sink.events[1], .cancelled)
+        XCTAssertEqual(sink.events[2], .secondaryValidationReturned)
+        XCTAssertEqual(sink.events[3], .resize(width: 800, height: 600))
+    }
+
+    func testWKWebViewShimExposesExpectedMethods() {
+        for method in ["LoginSuccess", "Cancel", "SecondaryValidationResult", "SwitchLogin"] {
+            XCTAssertTrue(AuthWebViewBridge.wkWebViewShim.contains(method))
+        }
+        XCTAssertTrue(AuthWebViewBridge.wkWebViewShim.contains("webkit.messageHandlers.livehime_login"))
+    }
+
+    func testLoginSuccessAcceptsTokenAlias() {
+        let sink = Sink()
+        let bridge = AuthWebViewBridge(delegate: sink)
+        bridge.loginSuccess(["type": "scan", "token": "opaque-token"])
+        guard case let .succeeded(result) = sink.events.first else {
+            return XCTFail("expected success event")
+        }
+        XCTAssertEqual(result.refreshToken, "opaque-token")
+    }
+}
diff --git a/plugins/livehime/core/Tests/LiveHimeCoreTests/SessionCoordinatorTests.swift b/plugins/livehime/core/Tests/LiveHimeCoreTests/SessionCoordinatorTests.swift
index 30e058beb..7c98f5c26 100644
--- a/plugins/livehime/core/Tests/LiveHimeCoreTests/SessionCoordinatorTests.swift
+++ b/plugins/livehime/core/Tests/LiveHimeCoreTests/SessionCoordinatorTests.swift
@@ -187,6 +187,23 @@ final class SessionCoordinatorTests: XCTestCase {
         XCTAssertEqual(c.state, .signedOut(.legacyNeedsLogin))
     }
 
+    func testWebLoginIsSavedThenVerified() async throws {
+        let store = MemoryCredentialStore()
+        let (c, states) = make(StubAPI(), store)
+        c.completeWebLogin(SessionCredentials(cookies: completeCookies, refreshToken: "web"))
+        await c.waitForIdle()
+        guard case .ready = c.state else { return XCTFail("got \(c.state)") }
+        XCTAssertEqual(states().first, .verifying)
+        guard case .current(let saved) = try store.load() else { return XCTFail("not saved") }
+        XCTAssertEqual(saved.refreshToken, "web")
+    }
+
+    func testWebLoginPayloadMayBeJSONString() {
+        XCTAssertEqual(WebLoginWindow.dictionary(#"{"refresh_token":"r"}"#)["refresh_token"] as? String, "r")
+        XCTAssertEqual(WebLoginWindow.dictionary(["a": 1])["a"] as? Int, 1)
+        XCTAssertTrue(WebLoginWindow.dictionary(nil).isEmpty)
+    }
+
     func testSignOutRemovesCredentials() async throws {
         let store = MemoryCredentialStore(.current(SessionCredentials(cookies: completeCookies, refreshToken: "")))
         let (c, _) = make(StubAPI(), store)
diff --git a/plugins/livehime/core/include/livehime-core.h b/plugins/livehime/core/include/livehime-core.h
index 7db954aa1..dee5292c4 100644
--- a/plugins/livehime/core/include/livehime-core.h
+++ b/plugins/livehime/core/include/livehime-core.h
@@ -21,6 +21,9 @@ void livehime_core_start_qr_login(void);
 void livehime_core_cancel_qr_login(void);
 void livehime_core_retry(void);
 void livehime_core_sign_out(void);
+/* Opens the official password / SMS login page (captcha and verification
+ * are handled by the page); success continues like a QR login. */
+void livehime_core_start_web_login(void);
 
 /* Live room. Results arrive through the same callback as JSON objects with
  * an "event" field instead of "state":
diff --git a/plugins/livehime/data/locale/en-US.ini b/plugins/livehime/data/locale/en-US.ini
index 7266f5fb2..9cba19fbc 100644
--- a/plugins/livehime/data/locale/en-US.ini
+++ b/plugins/livehime/data/locale/en-US.ini
@@ -118,3 +118,4 @@ LiveHime.Feature.AddKeyword="Add"
 LiveHime.Feature.RemoveKeyword="Remove selected"
 LiveHime.Feature.KeywordHint="Bilibili hides danmaku containing these words for everyone in your room."
 LiveHime.Feature.KeywordFailed="Blocked words could not be updated."
+LiveHime.Account.LoginWeb="Log in with password or SMS..."
diff --git a/plugins/livehime/data/locale/zh-CN.ini b/plugins/livehime/data/locale/zh-CN.ini
index 8741f0f10..b1b8732f4 100644
--- a/plugins/livehime/data/locale/zh-CN.ini
+++ b/plugins/livehime/data/locale/zh-CN.ini
@@ -118,3 +118,4 @@ LiveHime.Feature.AddKeyword="添加"
 LiveHime.Feature.RemoveKeyword="删除所选"
 LiveHime.Feature.KeywordHint="哔哩哔哩会为直播间里所有人隐藏包含这些词的弹幕。"
 LiveHime.Feature.KeywordFailed="屏蔽词未能更新。"
+LiveHime.Account.LoginWeb="改用密码或短信登录…"
diff --git a/plugins/livehime/src/account-panel.cpp b/plugins/livehime/src/account-panel.cpp
index c78251b3e..32484b66a 100644
--- a/plugins/livehime/src/account-panel.cpp
+++ b/plugins/livehime/src/account-panel.cpp
@@ -347,9 +347,13 @@ QWidget *AccountPanel::buildSignedOut()
 	signedOutNotice_->setStyleSheet(QStringLiteral("color: #e0a040;"));
 	auto *login = new QPushButton(text("LiveHime.Account.LoginQR"), page);
 	connect(login, &QPushButton::clicked, [] { livehime_core_start_qr_login(); });
+	auto *webLogin = new QPushButton(text("LiveHime.Account.LoginWeb"), page);
+	webLogin->setFlat(true);
+	connect(webLogin, &QPushButton::clicked, [] { livehime_core_start_web_login(); });
 	layout->addWidget(wrapLabel(page, text("LiveHime.Account.SignedOut")));
 	layout->addWidget(signedOutNotice_);
 	layout->addWidget(login);
+	layout->addWidget(webLogin);
 	return page;
 }
 
@@ -364,9 +368,13 @@ QWidget *AccountPanel::buildQRCode()
 	qrHint_->setAlignment(Qt::AlignCenter);
 	auto *cancel = new QPushButton(text("LiveHime.Account.Cancel"), page);
 	connect(cancel, &QPushButton::clicked, [] { livehime_core_cancel_qr_login(); });
+	auto *webLogin = new QPushButton(text("LiveHime.Account.LoginWeb"), page);
+	webLogin->setFlat(true);
+	connect(webLogin, &QPushButton::clicked, [] { livehime_core_start_web_login(); });
 	layout->addWidget(qrImage_);
 	layout->addWidget(qrHint_);
 	layout->addWidget(cancel);
+	layout->addWidget(webLogin);
 	return page;
 }
 
-- 
2.54.0 (Apple Git-157)

